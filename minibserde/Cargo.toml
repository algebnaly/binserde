[package]
name = "minibserde"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "serialization and deserialization library for binary format"

[dependencies]
minibserde-core = { path = "../minibserde-core" }
minibserde-derive = { path = "../minibserde-derive" }
